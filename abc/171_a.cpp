#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
using ll = long long;

constexpr ll INF = 1<<30;

int main()
{
  char ch;
  cin >> ch;
  if(isupper(ch)) cout << 'A' << '\n';
  else            cout << 'a' << '\n';
  return 0;
}
